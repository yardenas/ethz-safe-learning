models:
  mlp_ensemble:
    ensemble_size: 5
    n_epochs: 100
    batch_size: 512
    validation_split: 0.2
    learning_rate: 0.001
    mlp_params:
      n_layers: 2
      units: 250
      activation: tf.nn.relu
      dropout_rate: 0.0
